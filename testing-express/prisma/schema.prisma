generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "${DATABASE_ENGINE}"
  url      = env("DB_URL")
}

model User {
  id    Int     @default(autoincrement()) @id
  name  String?
  email String  @unique
}
